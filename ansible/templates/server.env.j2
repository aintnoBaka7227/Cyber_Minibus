# 🌐 Database
MONGODB_URI=mongodb://localhost:{{ mongodb_port | default(27017) }}

# JWT token (provide a strong secret via vars)
JWT_SECRET={{ jwt_secret }}

# Security toggles (defaults safer for prod)
VULNERABLE_SQLI_MODE={{ server_vulnerable_sqli_mode | default(false) | ternary('true','false') }}
VULNERABLE_MODE={{ server_vulnerable_mode | default(false) | ternary('true','false') }}

