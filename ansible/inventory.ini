###############################################
# Cyber_Minibus Ansible Inventory (Ronin)
#
# How to use
# - Set your default SSH user and key in [all:vars]
# - Put your Ronin VM(s) under the appropriate group
# - The playbook targets the [ronin] group by default
#
# Quick test:
#   ansible ronin -i ansible/inventory.ini -m ping
###############################################

[all:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=~/.ssh/minibus-test-phong.pem

[ronin]
# Single-VM deployment (alias → actual host)
ronin-vm ansible_host=minibus-test-phong.uoa.cloud

# --- Optional: Split-tier layout (example) ---
# If you later separate components, you can use:
#
#[frontend]
#frontend-vm ansible_host=front.example.com
#
#[backend]
#backend-vm ansible_host=api.example.com
#
#[database]
#db-vm ansible_host=db.example.com
#
# And change the playbook hosts accordingly (e.g., hosts: backend)

